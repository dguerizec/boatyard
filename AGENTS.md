# Agent Instructions

- Before adding a new component or UI mechanism, check whether it can reasonably reuse or extend an existing abstraction.
- When factorization is appropriate, factorize the shared behavior instead of duplicating it in the new component.
- Keep the factorization pragmatic: extract only when it reduces real duplication, clarifies ownership, or makes future components easier to add without broad rewrites.
